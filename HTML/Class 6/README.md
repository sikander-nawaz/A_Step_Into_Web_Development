# Class 6 - HTML Media Elements

This session focused on the `<audio>` and `<video>` tags in HTML and how they can be used to embed media content directly into a webpage. Interactive buttons were also implemented to enhance functionality, enabling controls such as play/pause and dynamic video resizing.
